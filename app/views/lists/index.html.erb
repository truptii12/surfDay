<p id="notice"><%= notice %></p>

<h1>Listing Lists</h1>

<table>
  <thead>
    <tr>
      <th>Dt</th>
      <th>Pressure</th>
      <th>Humidity</th>
      
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= list.dt %></td>
        <td><%= list.pressure %></td>
        <td><%= list.humidity %></td>
       <td> <li><a href="/temp">Temprature Details</a></li></td> 
        <!--td><!--%= link_to 'Show', list %></td-->
        <!--td><!--%= link_to 'Edit', edit_list_path(list) %></td-->
        <!--td><!--%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %></td-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New List', new_list_path %>
